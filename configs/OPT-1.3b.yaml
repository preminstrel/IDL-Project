mode: train

# data settings
dataset: imdb
batch_size: 5
num_workers: 8

# training settings
epochs: 100
lr: 0.002
resume: False
save_freq: 20
valid_freq: 1
seed: 1225

regularization: l2
weight_decay: 0.001

scheduler: cosine

# model
arch: facebook/opt-1.3b
soft_prompt_tokens: 100

# wandb settings
use_wandb: True
project: project
name: opt-1.3b
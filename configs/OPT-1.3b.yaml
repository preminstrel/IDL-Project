mode: train

# data settings
dataset: wikitext2
batch_size: 10
num_workers: 8

# training settings
epochs: 100
lr: 0.002
resume: False
save_freq: 20
valid_freq: 1
seed: 1225

regularization: l2
weight_decay: 0.001

scheduler: cosine

# model
arch: facebook/opt-1.3b
soft_prompt_tokens: 10

# wandb settings
use_wandb: True
project: project
name: opt-1.3b